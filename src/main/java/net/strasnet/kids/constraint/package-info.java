/**
 * @author Chris Strasburg
 *
 * Constraints represent languages which, when applied to a domain space with defining values, defines a partition of the
 * domain space into values (or ranges for continuous spaces). Partitions which are positively identified are
 * signal positive, while those which are negatively identified are signal negative.
 */
package net.strasnet.kids.constraint;